# A03 规格一致性验证记录

日期：2026-09-14；分支：`codex/analytics-a01`；版本：`a03-synthetic-v1`。

执行命令（项目根目录）：

```bash
python3 docs/ai/智能统计分析实施计划/A03-验证资产/verify-specification.py
```

验收对象是 [合成身份](synthetic-identities.json)、[预期场景](expected-scenarios.json)、[场景表](验收场景.md) 与 [A03 决策文档](../A03-统计授权与数据处理决策表.md)。

实际输出：

```text
PASS: 11 identities, 2 tenants, 3 organizations, 5 departments, 44 scenario specifications, 12 pending decisions; structural consistency only, no authorization executed.
```

| 验证项 | 实际结果 |
| --- | --- |
| 组织/身份引用 | 两租户、同租户两医院、单院三个科室齐备；工作上下文、统计和动作范围均引用合法组织 |
| 差异身份 | 单科室、多科室、全院、跨院/租户、临床无统计权、审核/目录无数据权、撤权和单指标身份齐备 |
| 默认能力 | 聚合优先；明细、导出、外部模型、语音及真实数据均关闭，无患者级默认输出维度 |
| 场景规格 | C01–C44 唯一连续、身份引用有效、JSON 与 Markdown 一致；含明确预期和理由 |
| 待决策 | D01–D12 均列明建议责任角色、阻塞范围与放行证据；没有正式责任人或签字被虚构 |
| 文档与范围 | 本地链接可解析，围栏完整；本轮只新增/修改计划与合成规格文件 |

**这些检查只证明规格资产结构与引用一致，不代表 44 条实际授权测试通过。** 没有运行鉴权策略引擎、查询/导出接口、隐私抑制实现或模型/语音路径。A08/A13/A21/A23/A24 等实现后应把这些预期转换为实际集成/安全测试。

本轮未新增 IAM 授权或应用配置，未连接真实数据、未运行 H2/Oracle/PostgreSQL/浏览器检查。语音公网回退为静态代码发现，未触发请求。未交付运行功能，也未启动、停止或检查 8080/5173 服务。具体院方制度和 D01–D12 确认仍待完成。
