# A03 合成授权验收场景

本表是未来实现的预期行为规格，尚未运行实际鉴权。ALLOW 仅表示通过本场景关注的授权门禁，仍需目录、预算、隐私和审计检查。

| 编号 | 身份 | 请求 / 条件 | 预期 | 理由 / 范围 |
| --- | --- | --- | --- | --- |
| C01 | U_D1 | 本工作科室运行 M07 | ALLOW | 仅 T_A/H_A/D_A1；仍须预算、隐私与审计通过 |
| C02 | U_MULTI | 工作科室 D_A1，显式查询 D_A1+D_A2 的 M07 | ALLOW | 统计授权覆盖两个科室，不由当前工作科室缩成一个 |
| C03 | U_D1 | 显式查询 D_A2 | DENY | 显式范围超授权，不静默返回 D_A1 |
| C04 | U_MULTI | 显式查询 D_A1+D_A3 | DENY | 混合请求包含未授权科室，整体拒绝 |
| C05 | U_HOSP | 请求指定医院 H_A 全院 M07 | ALLOW | 仅 D_A1+D_A2+D_A3，不包含 H_B |
| C06 | U_HOSP | 声称全院即整个 T_A，查询 H_B | DENY | 同租户不等于同机构 |
| C07 | U_HB | 在本人 H_B 工作上下文查 D_B1 | ALLOW | 仅 T_A/H_B/D_B1 |
| C08 | U_HOSP | 传 T_B/H_C/D_C1 | DENY | 租户从服务端身份取得，不能覆盖 |
| C09 | U_TB | 查询自己 H_C/D_C1 | ALLOW | 仅 T_B/H_C/D_C1 |
| C10 | U_CLIN | 有临床工作上下文但无统计动作，运行 M07 | DENY | 临床权限不推出统计权限 |
| C11 | U_CATALOG | 编辑 H_A 候选指标定义 | ALLOW_METADATA_ONLY | 目录管理授权只含候选定义，不含业务结果 |
| C12 | U_CATALOG | 运行全院 M07 | DENY | 管理目录不授予 RUN 或数据范围 |
| C13 | U_REVIEW | 审核其他作者的 D_A1 科室模板 | ALLOW_METADATA_ONLY | 范围匹配；不附带作者查询结果 |
| C14 | U_REVIEW | 审核 D_A2 或 H_A 全院模板 | DENY | 审批动作还必须匹配审核对象范围 |
| C15 | U_REVIEW | 审核自己提交的 D_A1 模板 | DENY | 合成职责分离策略不允许自审 |
| C16 | U_D1 | 读取 AS_SHARED 后按当前授权运行 M07 | ALLOW | 读取模板不借 U_HOSP 权限；结果标明 D_A1/部分科室 |
| C17 | U_MULTI | 读取他人的 AS_PRIVATE | DENY | 同科室仍不拥有个人草稿访问权；不泄露对象是否存在 |
| C18 | U_TB | 按 ID 读取 AS_SHARED | DENY | 资产 ACL 不跨租户 |
| C19 | U_EXPORT | 有 DETAIL 动作，要求患者明细 | DENY | 默认 detail_enabled=false，且无目的/字段专项批准 |
| C20 | U_EXPORT | 有 EXPORT 动作，要求聚合下载 | DENY | 默认 export_enabled=false，动作不足以开放能力 |
| C21 | U_LIMIT | 运行已获授权的 M07 | ALLOW | 仅 M07；输出受隐私等下游校验 |
| C22 | U_LIMIT | 运行未获授权的 M01 | DENY | 指标不在授权集合 |
| C23 | U_LIMIT | 要求 M12，称只是 M07 的简单环比 | DENY | 派生指标本身未获授权，不能由底层指标授权自动扩展 |
| C24 | U_HOSP | 真实目录运行仍为 CANDIDATE 的 M01 | DENY | 目录未审核；合成假设不改变真实状态 |
| C25 | U_HOSP | 运行 M13 或未治理复诊率 | DENY | MPI/复诊规则未治理，管理者也不能绕过 |
| C26 | U_D1 | 目的改为未批准的 MARKETING | DENY | 非 OPERATIONS_REVIEW，不能以字符串声明自行获批 |
| C27 | U_REVOKED | 提交新运行 | DENY | 授权已撤销，即使动作字符串仍存在 |
| C28 | U_MULTI | 排队后执行前撤销 D_A2，仍请求两个科室 | DENY | 执行前重查；不沿用队列中的旧授权 |
| C29 | U_MULTI | 执行中撤销 D_A2 | CANCEL_AND_BLOCK_DELIVERY | 尝试取消数据库语句，取消未成功也禁止交付结果 |
| C30 | U_MULTI | 结果读取前撤销 D_A2 | DENY | 旧结果不可按缩小后的范围直接复用，应重新查询 |
| C31 | U_MULTI | 缓存命中前撤销 D_A2 | DENY | 缓存不是授权凭证，重新解析范围与策略 |
| C32 | U_EXPORT | 专项批准导出情境下，下载前撤权 | DENY | 即使此前任务和凭证有效，下载时仍拒绝；默认开关未因此开启 |
| C33 | U_D1 | 策略服务或必要审计不可用 | FAIL_CLOSED | 不执行、不交付；不回退到无保护查询 |
| C34 | U_D1 | 未批准路径发送原始需求给外部模型 | DENY | external_model_enabled=false；文本可能含患者信息 |
| C35 | U_D1 | 标为院内的语音配置实际请求公网端点 | DENY | speech_enabled=false；后续还须核对实际提交与轮询链路 |
| C36 | U_D1 | 仅合成隐私示例：分组 3 和 9，总计 12，k=5 | PROTECT | 隐藏 3 及可相减恢复它的总计 12；不把原值放在图表/下载/摘要 |
| C37 | U_D1 | 连续变更相邻筛选以恢复被抑制小群体 | DENY_OR_REQUIRE_REVIEW | 拒绝/限制相关维度并记录风险；不能仅靠单次阈值 |
| C38 | U_MULTI | scopeIntent=当前授权范围，不显式要求全院 | ALLOW | 仅在 H_A 内展开 D_A1+D_A2，并标注部分科室 |
| C39 | U_D1 | scopeIntent=全院且要求完整 H_A | DENY | 缺完整授权，提示可改查当前授权范围，但不自动更改需求 |
| C40 | U_D1 | 请求字段 patient_id 或按患者标识分组 | DENY | 普通聚合目录不允许展示标识或患者级维度 |
| C41 | U_D1 | 在比较期/关联支路加入 D_A2 | DENY | 所有支路同受数据策略，不在聚合后前端隐藏 |
| C42 | U_D1 | 在 valid_until_exclusive 时刻运行 | DENY | 授权区间右端不包含，过期即拒绝 |
| C43 | U_D1 | 伪造工作上下文为 H_B/D_B1 | DENY | 先验证工作上下文，不依据请求头扩大统计授权 |
| C44 | U_D1 | 允许范围内确认无事实数据的月份 | ALLOW_EMPTY | 预算/质量/隐私通过后返回空结果，不能把拒绝伪装成零 |
