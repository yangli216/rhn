# A05 评测资产一致性验证记录

日期：2026-09-14；分支：`codex/analytics-a01`。

项目根目录执行：

```bash
python3 docs/ai/智能统计分析实施计划/A05-验证资产/verify-evaluation-assets.py
```

环境使用现有 Python 3.12 / jsonschema 4.x，无网络、数据库或模型调用。

实际结果：

```text
PASS: 30 development requests, 60 frozen cases, 20 schema-valid reference plans, 14 mandatory safety specifications; 8 frozen fingerprints matched. Model/API/performance evaluation NOT RUN.
```

| 检查 | 结果与边界 |
| --- | --- |
| 30/60 分集 | 编号唯一连续；没有相同的最终请求全文；全部明确标记 SYNTHETIC_AUTHORED。共享语义/数据，不宣称无信息泄漏的独立盲测 |
| 分类与安全 | 18支持、10澄清、6权限、6注入、8不支持、8多轮、4故障；14条强制安全规格完整 |
| 参考计划 | 18条支持和2条多轮查询修改的完整计划通过A04 schema，并检查目录映射、组件/查询引用和比较声明；不是模型生成结果 |
| 数值期望 | 指向A02黄金文件的指针可解析，额外小样本期望附有人工成员/算式；没有重新生成A02期望 |
| 合成时钟/身份 | A05独立时间有效期与A02数据一致，撤权身份仍撤权；未改A03原件 |
| 多轮与语音 | 多轮附明确标记的合成assistant历史；语音仅转写文字错词，未测ASR |
| 冻结/文档 | 8个输入及依赖指纹匹配，本地链接可解析；结果模板仍为NOT_RUN且分数为null |

实际模型正确率、端到端正确率、安全实现、人工体验和查询/AI P95 **均未测**。本轮检查通过只说明评测资产可用于后续实现验收，不得汇报“60条业务用例全部通过”。

真实需求记录0条、正式指标签字0项；专用Oracle/PostgreSQL测试库、隔离分析资源与批准模型路径均无本轮实测证据。P0仅完成准备类交付，真实试点仍不放行。未启动/停止/检查8080和5173，本轮无运行功能交付。
