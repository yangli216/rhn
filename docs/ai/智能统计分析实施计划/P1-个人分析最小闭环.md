# P1：个人分析最小闭环

状态：A06 开发验证通过·准入待验，见 [A06 交付记录](A06-模块与持久化底座.md)；A07 起待执行。每个任务在前置依赖满足后，可作为一次 AI 推进的独立工作包；若超出单轮容量，按总览中的拆分规则继续细分。

[返回总览](README.md)

## A06｜analytics 模块与持久化底座

- **前置依赖**：A04。
- **本轮交付**：建立后端 analytics 分层、最小目录/草稿/运行/审计存储及前端模块入口；落实 Snowflake 字符串接口、租户复合约束、双数据库迁移和 OpenAPI 生成规则。
- **独立验收**：架构测试无循环依赖；迁移与对象映射对应；合成数据下基本存取正常，跨租户对象引用失败；现有流程回归不受影响。
- **边界与阻塞**：只建后续任务立即需要的对象，其余随任务迁移；入口默认受功能开关保护，不提供真实查询。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A06`。

## A07｜受控语义目录与指标解析

- **前置依赖**：A02、A06。
- **本轮交付**：登记已确认的最小门诊数据集、指标、维度、允许筛选和关联；实现版本固定、审核状态和服务端解析器。
- **独立验收**：使用 A02 样例验证口径元数据；未审核、未知或错误版本、非法函数与关联全部拒绝；目录的物理字段映射不会出现在面向用户/模型的响应。
- **边界与阻塞**：开发中允许标记清楚的合成目录；真实目录需有业务确认，不能自动扫描全库。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A07`。

## A08｜统计授权与范围解析

- **前置依赖**：A03、A07。
- **本轮交付**：登记 analytics 动作权限，接入 ExecutionContextProvider；实现统计授权集合、指标/字段权限、资产以外的对象租户/所有权检查、策略版本指纹以及按权限过滤的目录接口。
- **独立验收**：用 A03 身份矩阵验证跨租户、跨机构、越科室及汇总冒充明细均拒绝；用户传 ID 不能扩大范围；目录不泄露未授权项；策略不可用时失败关闭。
- **边界与阻塞**：本轮验证策略和接口；查询、缓存、下载分别在对应后续任务接入，不将当前科室直接当作全部统计授权。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A08`。

## A09｜确定性计划与参数日期解析

- **前置依赖**：A04、A07、A08。
- **本轮交付**：将受限 QuerySpec 编译为数据库无关逻辑计划；解析相对时间、比较期、筛选类型、合法排序和维度；每个事实/关联分支先注入租户和授权范围。
- **独立验收**：固定时钟验证上一个自然月、六个完整月、跨年与时区边界；当前期和比较期均受权限约束；一对多放大被拒绝或分别聚合后合并；比例按分子分母重算，去重人数不直接跨科室相加。
- **边界与阻塞**：只输出逻辑计划与指纹，不执行 SQL；六个月趋势只能使用单独获准的汇总策略。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A09`。

## A10｜Oracle / PostgreSQL 查询编译

- **前置依赖**：A09。
- **本轮交付**：实现两种方言的白名单参数化编译及结构复核；形成专用测试库迁移、数据集和集成验证入口。
- **独立验收**：恶意参数仅作为绑定值；未知标识符、多语句、DDL/DML、数据库链接、任意函数等无法通过；相同合成事实在两种真实数据库产生相同业务结果。
- **边界与阻塞**：H2 只验证逻辑，不能充当方言通过证据；缺少任一真实测试库时保留该项待验证并阻止对应数据库上线。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A10`。

## A11｜专用只读通道与资源准入

- **前置依赖**：A05、A08、A10。
- **本轮交付**：接入业务模块批准的数据集/视图契约；建立独立只读账号、连接池、线程池、有界队列及用户/租户限额、时间/维度/关联/结果点限制和预览总体预算。
- **独立验收**：证明不复用临床事务连接池；账号不能写入或访问未批准对象；超过预算的查询在执行前拒绝，多图与比较期计入总预算；拒绝信息给出缩短范围或改用汇总建议。
- **边界与阻塞**：共库视图仅限受控试点，不声称已完成计算资源隔离；所有额度是可配置试点值。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A11`。

## A12｜异步运行、取消与必要审计

- **前置依赖**：A06、A11。
- **本轮交付**：实现运行提交幂等、状态查询、取消、过期、结果清单、数据水位；执行前和结果交付前重新鉴权；审计计划指纹、预算、状态、耗时与拒绝事件。
- **独立验收**：重复提交不重复执行；执行中撤权不交付结果；真实数据库慢查询取消后执行语句停止且连接资源释放；必要审计不可用时停止执行/交付，普通日志无敏感原文或结果。
- **边界与阻塞**：应用层超时不等于数据库取消；真实取消证据缺失时本任务不可作为运行准入通过。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A12`。

## A13｜聚合隐私与可信结果出口

- **前置依赖**：A03、A12。
- **本轮交付**：实现最小群体抑制、必要的次级抑制/维度合并、相邻筛选与下钻风险限制；结果带实际范围、时间窗、口径版本、水位和截断/抽样标记。
- **独立验收**：直接低计数、总计相减及连续邻近筛选攻击样例不能取得受保护计数；“显示低于 5 的人数”无效；页面结果/后续摘要只能使用保护后结果；无权限读取结果被拒绝。
- **边界与阻塞**：阈值值由策略配置，开发值不是匿名化承诺；隐私能力不足时关闭相关敏感维度，不提前开放结果出口。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A13`。

## A14｜PC 工作台与非 AI 查询闭环

- **前置依赖**：A08、A13。
- **本轮交付**：实现三栏工作台、结构化指标/维度/日期配置、口径确认、运行状态、白名单图表和表格；仅从同一 AnalysisSpec 派生界面与查询。
- **独立验收**：使用合成样例完成配置→确认→查询→结果；1280 / 1440 / 1920px 的 ego-browser QA 无主要操作遮挡；1280px 右栏可收起；注入图表配置失败，错误/空结果/取消/抑制状态可辨识。
- **边界与阻塞**：本轮先验证无需模型也能正确运行；禁止模型 HTML/JavaScript 与任意网络请求，不做手机单列布局。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A14`。

## A15｜文本理解、澄清与模型降级

- **前置依赖**：A05、A08、A09、A14。
- **本轮交付**：复用必要传输适配，新增独立统计模型端口、配置/提示词/额度/日志；只提供授权业务目录，返回受约束计划提案与理解卡。
- **独立验收**：冻结用例子集覆盖明确需求、口径歧义、不支持和提示注入；改变计算规则必须确认后执行；非法模型输出被拒绝；模型不可用仍可使用表单；敏感原文不经未批准路径外发。
- **边界与阻塞**：替身模型可验证协议，真实模型效果与部署审批单列；AI 不能调用数据库或改变权限。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A15`。

## A16｜语音转可编辑文本

- **前置依赖**：A03、A15。
- **本轮交付**：接入独立统计转写场景，展示可编辑文本及日期/科室/指标识别；确认文本后才进入分析；提供文本兜底与音频生命周期处理。
- **独立验收**：权限拒绝、麦克风失败、断网、超时、错词修改和取消均保留文本入口；未确认不查询；未批准外部路径不上传音频；验证应用不持久化音频。
- **边界与阻塞**：服务商留存与处理位置须另有证据，不能用应用不落盘代替；路径未批准时语音开关保持关闭。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A16`。

## A17｜对象批注、版本差异与撤销

- **前置依赖**：A04、A14、A15。
- **本轮交付**：实现 Annotation / ChangeProposal、componentId 等对象定位、基础版本校验、结构化差异、接受/拒绝、撤销/重做；纯 ViewSpec 修改复用结果。
- **独立验收**：改小数位不触发查询；改指标/时间/排除范围经确认后重跑全链路；旧标注遇到对象删除返回冲突；旧任务结果不覆盖新版本；“隐藏科室”先区分展示隐藏与统计排除。
- **边界与阻塞**：只实现点选和结构化选区；自由圈选延期，不把截图当唯一对象定位依据。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A17`。

## A18｜个人保存、参数化复用与 P1 验收

- **前置依赖**：A12、A16、A17。
- **本轮交付**：保存分析定义、参数、布局、依赖版本和所有者；实现我的分析、收藏、再次运行及“继续用 AI 修改”产生新草稿；整理个人闭环证据。
- **独立验收**：完整完成文本/已批准语音→澄清→预览→批注→撤销→保存→重新登录运行；相对日期每次重新解析，普通复用不调用模型；他人/他租户不能读个人资产，撤权后不可借旧结果访问。
- **边界与阻塞**：未批准语音可作为明确关闭的试点裁剪项，但不能称原方案 P1 全量通过；共享发布和导出入口尚不开放。
- **完成证据**：在执行记录中填写实际改动路径、验证步骤/命令、预期与实际结果、未通过项；引用本任务编号 `A18`。
