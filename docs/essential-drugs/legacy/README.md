# V1.47 历史生成输入

这里保留远程提交 c0781b2 对应的旧目录 JSON 与 2,197 条规格导入表，仅用于重现 V1.47 预置迁移。其自动生成的临床默认属性尚需实际调试核对，不等同于上级目录中的标准参考目录或已核验的成分语义。

运行 `node scripts/build-essential-drugs-artifacts.mjs` 会将对照产物写入忽略的 `output/essential-drugs-legacy/`，不修改历史迁移。新增目录维护继续使用上级目录的标准目录生成器。
