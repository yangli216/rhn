spring:
  datasource:
    url: ${RHN_ORACLE_URL}
    username: ${RHN_ORACLE_USER}
    password: ${RHN_ORACLE_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      # Oracle reports compatible CLOB/text mappings differently from Hibernate.
      # Flyway remains the authoritative schema contract for this development profile.
      ddl-auto: none
    properties:
      hibernate:
        query:
          # Secondary-table entities otherwise make Hibernate create HT_/HTE_ global
          # temporary tables at startup. Existing code has no multi-table HQL insert.
          mutation_strategy: org.hibernate.query.sqm.mutation.internal.inline.InlineMutationStrategy
          mutation_strategy.global_temporary.create_tables: false
  flyway:
    locations: classpath:db/oracle,classpath:db/oracle-local
    # Local demo datasets may be added behind the current schema version during parallel iteration.
    out-of-order: true
    validate-on-migrate: false

rhn:
  id:
    allow-default-node: true
  security:
    dev-user-enabled: true
    dev-username: ${RHN_DEV_USERNAME:doctor}
    dev-password: ${RHN_DEV_PASSWORD:rhn-dev-2026}
    session-registry: ${RHN_SESSION_REGISTRY:memory}
    refresh-login:
      cookie-secure: false
  crypto:
    active-provider: development-jca
    enforce-compliance: false
  presence:
    store: ${RHN_PRESENCE_STORE:memory}

springdoc:
  api-docs:
    enabled: true

management:
  health:
    redis:
      enabled: false
